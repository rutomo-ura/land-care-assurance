StatusPill labels a record's state inline in a table row or card.

```jsx
<StatusPill tone="submitted">Submitted</StatusPill>
<StatusPill tone="risk">Needs Action</StatusPill>
```

Never rely on the pill's color alone — always pair with the status word, per LandCare's accessibility rule (status must be understandable without color vision).
